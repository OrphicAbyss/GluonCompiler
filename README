================================================================================
GLUON LANGUAGE  -  Alpha
================================================================================

Compiler I'm building for a language I'm making up as I go. Current it's a mix
of basic, c and pascal.

Working so far:
* Integer maths
* Integer variables
* If statements (with else)
* For loops (c like syntax)
* While loop
* Break
* Function defs (Can parse with multiple input parameters and multiple return values but code output ignores inputs and outputs
* Function calls (With no parameters or return values)
